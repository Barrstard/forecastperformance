# Database Configuration
POSTGRES_PASSWORD=your_secure_postgres_password_here

# Application Configuration  
DATABASE_URL=postgresql://forecast_user:your_secure_postgres_password_here@postgres:5432/forecastperformance
REDIS_URL=redis://redis:6379

# NextAuth Configuration
NEXTAUTH_URL=http://your-unraid-ip:3000
NEXTAUTH_SECRET=your_nextauth_secret_here

# BigQuery Configuration (encrypted and stored in database)
# Upload your service account JSON through the web interface

# Application Environment
NODE_ENV=production

# Optional: Custom port if you want to change from 3000
# CUSTOM_PORT=3000